// Import the User and Thought models from the current directory
import User from "./User.js";
import Thought from "./Thought.js";

// Export the User and Thought models for use in other parts of the application
export { User, Thought };
